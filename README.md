# FastAPI Notes API

Производственное REST API для управления заметками с JWT-аутентификацией. Проект демонстрирует современные практики Python back-end разработки.

## 🚀 Быстрый старт

### Предварительные требования
- Docker и Docker Compose
- Или Python 3.11+ и PostgreSQL 15+

### Запуск через Docker (рекомендуется)

# 1. Клонируйте репозиторий
git clone <ваш-репозиторий>
cd <папка-проекта>

# 2. Настройте переменные окружения
cp .env.example .env
# Отредактируйте .env (укажите свои пароли)

# 3. Запустите сервисы
docker-compose up -d --build

# 4. API будет доступно по адресу: http://localhost:8000
#    Документация: http://localhost:8000/docs

Локальный запуск

# 1. Установите зависимости
pip install -r requirements.txt

# 2. Настройте .env файл
cp .env.example .env

# 3. Примените миграции
alembic upgrade head

# 4. Запустите сервер
uvicorn app.main:app --reload

🔧 Технологический стек

Основные технологии
FastAPI 0.104 - современный асинхронный веб-фреймворк

SQLAlchemy 2.0 - ORM с асинхронной поддержкой

PostgreSQL 15 - производственная база данных

Alembic - управление миграциями базы данных

Pydantic 2.0 - валидация данных и настройки

JWT - аутентификация через OAuth2

Безопасность
Хеширование паролей с bcrypt

JWT токены с HS256 алгоритмом

Защита эндпоинтов через зависимости FastAPI

Переменные окружения для конфиденциальных данных

Инфраструктура
Docker контейнеризация

Docker Compose для оркестрации

Автоматические миграции при запуске

📡 API Эндпоинты
Аутентификация
POST /api/v1/auth/signup - Регистрация пользователя

POST /api/v1/auth/login - Вход и получение JWT токена

Заметки (требуют аутентификации)
GET /api/v1/notes/ - Список заметок пользователя

POST /api/v1/notes/ - Создание заметки

GET /api/v1/notes/{id} - Получение заметки по ID

PUT /api/v1/notes/{id} - Обновление заметки

DELETE /api/v1/notes/{id} - Удаление заметки

🧪 Запуск тестов

# Установите тестовые зависимости
pip install pytest pytest-asyncio httpx

# Запустите тесты
pytest app/tests/ -v

🔍 Проверка качества кода
Проект настроен с использованием современных инструментов:

# Форматирование кода
black app/

# Проверка типов
mypy app/

# Линтинг
flake8 app/

# Сортировка импортов
isort app/

⚠️ Важные замечания

Для production использования:
Замените SECRET_KEY в .env на криптографически безопасный ключ

Настройте правильные CORS политики

Используйте HTTPS

Настройте мониторинг и логирование